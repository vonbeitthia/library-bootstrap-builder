import { BsRuedaratonDirective } from './bs-ruedaraton.directive';

describe('BsRuedaratonDirective', () => {
  it('should create an instance', () => {
    const directive = new BsRuedaratonDirective();
    expect(directive).toBeTruthy();
  });
});
