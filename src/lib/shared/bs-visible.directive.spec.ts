import { BsVisibleDirective } from './bs-visible.directive';

describe('BsVisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new BsVisibleDirective();
    expect(directive).toBeTruthy();
  });
});
