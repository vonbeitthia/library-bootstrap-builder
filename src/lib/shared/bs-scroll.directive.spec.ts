import { BsScrollDirective } from './bs-scroll.directive';

describe('BsScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new BsScrollDirective();
    expect(directive).toBeTruthy();
  });
});
