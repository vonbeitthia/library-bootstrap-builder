import { DomContainerDirective } from './dom-container.directive';

describe('DomContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new DomContainerDirective();
    expect(directive).toBeTruthy();
  });
});
